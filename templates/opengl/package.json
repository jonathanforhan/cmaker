{
  "name": "--CMAKER_REPLACE",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "./scripts.py build-debug",
    "release-build": "./scripts.py build-release",
    "dev-run": "yarn build; cd bin/debug; ./--CMAKER_REPLACE",
    "release-run": "yarn release; cd bin/release; ./--CMAKER_REPLACE",
    "clean": "./scripts.py clean"
  }
}
